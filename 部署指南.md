# Quartz 知识库部署指南

## 🎉 恭喜！你的 Quartz 知识库已经搭建完成

你的知识库现在已经在本地成功运行，可以通过 http://localhost:8080 访问。

## 📋 当前状态

✅ **已完成的配置**:
- Node.js 环境: v24.12.0
- Quartz 版本: v4.5.2
- 中文支持: 已启用 (locale: zh-CN)
- 数学公式: 已启用 (KaTeX)
- 代码高亮: 已启用
- 图谱视图: 已启用
- 示例内容: 已创建 4 个示例文件

✅ **已创建的示例文件**:
- `content/index.md` - 首页
- `content/数学公式示例.md` - 数学公式演示
- `content/代码示例.md` - 代码高亮演示
- `content/Markdown 语法.md` - Markdown 语法指南

## 🚀 部署到 Vercel/Netlify

### 准备工作

在部署之前，你需要：

1. **创建自己的 GitHub 仓库**
   - 登录 GitHub
   - 创建一个新的仓库（例如：`my-quartz-blog`）
   - 可以设置为 Public 或 Private

2. **配置 Git 远程仓库**

当前 Git 仓库指向的是 Quartz 官方仓库。你需要将其改为指向你自己的仓库。

### 步骤 1: 重新配置 Git 仓库

在项目目录 `d:\fuckwebot` 中执行以下命令：

```bash
# 移除当前的远程仓库配置
git remote remove origin

# 添加你自己的 GitHub 仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 或者使用 SSH（如果你配置了 SSH 密钥）
git remote add origin git@github.com:你的用户名/你的仓库名.git
```

### 步骤 2: 提交并推送代码

```bash
# 添加所有文件到暂存区
git add .

# 创建提交
git commit -m "Initial commit: Quartz 知识库初始化"

# 推送到 GitHub（首次推送需要设置上游分支）
git push -u origin v4
```

### 步骤 3: 部署到 Vercel

#### 3.1 注册并登录 Vercel

1. 访问 [Vercel 官网](https://vercel.com)
2. 使用 GitHub 账号登录
3. 授权 Vercel 访问你的 GitHub 仓库

#### 3.2 导入项目

1. 点击 "Add New Project"
2. 选择你刚才创建的 GitHub 仓库
3. 配置构建设置：
   - **Framework Preset**: 选择 "Other"
   - **Build Command**: `npx quartz build`
   - **Output Directory**: `public`
   - **Install Command**: `npm install`

4. 点击 "Deploy" 开始部署

#### 3.3 等待部署完成

- 首次部署通常需要 2-5 分钟
- 部署成功后，Vercel 会提供一个免费域名（如 `your-project.vercel.app`）
- 你可以在项目设置中绑定自定义域名

### 步骤 4: 部署到 Netlify（可选）

如果你选择使用 Netlify 而不是 Vercel：

#### 4.1 注册并登录 Netlify

1. 访问 [Netlify 官网](https://netlify.com)
2. 使用 GitHub 账号登录
3. 授权 Netlify 访问你的 GitHub 仓库

#### 4.2 导入项目

1. 点击 "Add new site" → "Import an existing project"
2. 选择 "Deploy with GitHub"
3. 选择你的仓库
4. 配置构建设置：
   - **Branch to deploy**: `v4`
   - **Build command**: `npx quartz build`
   - **Publish directory**: `public`

5. 点击 "Deploy site" 开始部署

## 📝 后续使用指南

### 如何添加新笔记

1. 在 `content/` 目录下创建新的 `.md` 文件
2. 添加 frontmatter（可选但推荐）：

```markdown
---
title: 笔记标题
date: 2025-12-24
tags:
  - 标签1
  - 标签2
---

# 笔记内容

这里是你的笔记内容...
```

### 如何链接笔记

使用 Obsidian 风格的双方括号链接：

```markdown
[[另一个笔记的文件名]]
[[笔记文件名|显示的文本]]
```

### 本地预览和开发

在添加或修改内容后，可以在本地预览：

```bash
# 构建并启动本地服务器
node quartz/bootstrap-cli.mjs build --serve

# 访问 http://localhost:8080 查看效果
```

服务器会自动监听文件变化并重新构建。

### 更新网站内容

当你添加或修改了笔记后，需要推送到 GitHub 来更新网站：

```bash
# 添加修改的文件
git add .

# 创建提交
git commit -m "更新笔记内容"

# 推送到 GitHub
git push
```

推送后，Vercel/Netlify 会自动检测到更改并重新部署（通常 1-2 分钟内完成）。

## 💡 常见问题和技巧

### 如何添加图片

1. 将图片放在 `content/` 目录下（可以创建子文件夹如 `content/images/`）
2. 在 Markdown 中使用相对路径引用：

```markdown
![图片描述](./images/example.png)
```

### 如何使用数学公式

- **行内公式**: `$E=mc^2$`
- **块级公式**:

```markdown
$$
\int_0^\infty e^{-x^2} dx = \sqrt{\pi}
$$
```

### 如何组织笔记

建议的目录结构：

```text
content/
├── index.md              # 首页
├── 技术/                 # 技术相关笔记
│   ├── Python.md
│   └── JavaScript.md
├── 学习/                 # 学习笔记
│   └── 数学.md
└── images/              # 图片资源
    └── example.png
```

### 如何自定义网站配置

编辑 `quartz.config.ts` 文件可以自定义：

- **网站标题**: 修改 `pageTitle`
- **网站域名**: 修改 `baseUrl`（部署后更新为你的实际域名）
- **主题颜色**: 修改 `theme.colors` 中的颜色值
- **字体**: 修改 `theme.typography` 中的字体设置

## 📚 有用的资源

- **Quartz 官方文档**: <https://quartz.jzhao.xyz/>
- **Markdown 语法指南**: <https://www.markdownguide.org/>
- **KaTeX 数学公式**: <https://katex.org/docs/supported.html>
- **Obsidian 帮助文档**: <https://help.obsidian.md/>

## 🎯 下一步行动

1. **创建 GitHub 仓库** - 在 GitHub 上创建一个新仓库
2. **配置远程仓库** - 按照步骤 1 的说明配置 Git 远程仓库
3. **推送代码** - 将代码推送到 GitHub
4. **部署到 Vercel/Netlify** - 按照步骤 3 或 4 的说明部署网站
5. **开始写作** - 在 `content/` 目录添加你的笔记

## ✨ 总结

你的 Quartz 知识库已经完全配置好了！现在你可以：

- ✅ 在本地预览网站（http://localhost:8080）
- ✅ 使用中文内容
- ✅ 编写数学公式
- ✅ 添加代码示例
- ✅ 查看笔记关系图谱
- ✅ 准备部署到互联网

祝你使用愉快！🚀

---

*如有问题，请参考 Quartz 官方文档或查看示例文件。*
